## 🧩 1. Общая информация

- **Название проекта**: AIR_WAFFLE_FINANCE (Desktop + Telegram Bot + Mini App + API)
- **Краткое описание**: Единая финансовая система для сети Air Waffle. Включает:
  - настольное приложение (операционный учет, отчеты кассира, справочники);
  - Telegram-бот (быстрое добавление, отчеты, доступ по правам);
  - Telegram Mini App (веб-интерфейс для ленты, операций и аналитики);
  - FastAPI-бэкенд (API для фронтенда и аналитики);
  - SQLite база данных (единый источник данных).
- **Основные функции**:
  - Лента операций Timeline (расходы, приходы, переводы, инкасации).
  - Отчеты кассира, корректировки и аудит.
  - Быстрое добавление операций (Desktop/Telegram/Mini App).
  - Справочники (счета, категории расходов/приходов).
  - Аналитика (KPI, сводные таблицы, тренды).
  - Права доступа (permissions) для Telegram и Desktop UI.
- **Технологический стек**:
  - Backend: Python 3, FastAPI, `uvicorn`, `python-dotenv`, SQLite.
  - Desktop: Python, Tkinter.
  - Telegram Bot: Python, `aiogram` (FSM), кастомная permissions-модель.
  - Frontend (Mini App): Vite, React, Tailwind CSS, `react-router-dom`, Axios, Recharts, Telegram WebApp API.

⸻

## 🗂️ 2. Структура проекта

Корень проекта:
- `main_app.py` — Desktop-приложение (Tkinter), главные окна и навигация.
- `settings.py`, `settings_gui.py` — настройки и UI настроек Desktop.
- `cashier_daily_report.py`, `manager_view.py` — логика отчетности кассира и менеджера.
- `telegram_bot.py`, `telegram_bot_simple.py` — Telegram-бот (FSM, быстрые операции, отчеты).
- `bot_db.py` — доступ к БД для бота (пользователи, права, timeline).
- `database_v5.py` — вспомогательная логика работы с БД (legacy).
- `api_server.py` — вспомогательный сервер/скрипты интеграции (если используется).
- `finance_v5.db` — основная SQLite база данных.
- `validation.py`, `setup_v5.py` — служебные/установочные скрипты.
- Текстовые инструкции: `ИНСТРУКЦИЯ_V5.txt`, `ФИНАЛЬНАЯ_ВЕРСИЯ.txt`, `GUI_ИНСТРУКЦИЯ.txt`, `TELEGRAM_БОТ.txt`.

Бэкенд:
- `backend/main.py` — FastAPI-приложение, все эндпоинты (timeline, операции, справочники, аналитика, редактирование).
- `backend/database.py` — подключение к SQLite, `db_session()`, конфигурация пути БД (`FINANCE_DB_PATH` или абсолютный default).
- `backend/auth.py` — аутентификация по заголовку `X-Telegram-Id` (в разработке; временно отключена под тестирование).
- `backend/analytics.py` — бизнес-аналитика: KPI (dashboard), сводная таблица (pivot), тренды (trend).
- `backend/requirements.txt` — зависимости Python.

Фронтенд (Mini App):
- `miniapp/frontend/`
  - `index.html`, `vite.config.js`, `tailwind.config.js`, `postcss.config.js`, `.env` (рекомендуется).
  - `src/main.jsx`, `src/index.css` — точка входа и стили.
  - `src/services/api.js` — Axios-клиент с интерсептором `X-Telegram-Id`, методы API.
  - `src/hooks/useTelegram.js` — обертка над Telegram WebApp API (user, haptics, alert).
  - `src/App.jsx` — роутинг, плавающая кнопка, вызов AddModal, нижняя навигация.
  - `src/pages/`
    - `Home.jsx` — главная карточки дня и баланс.
    - `Timeline.jsx` — лента операций с фильтрами, редактирование/удаление.
    - `Analytics.jsx` — вкладки Dashboard, PivotTable, ChartsView.
    - `Profile.jsx` — профиль (заглушка).
    - `Settings.jsx` — управление категориями/счетами (CRUD).
  - `src/components/Layout/` — `Header.jsx`, `BottomNav.jsx`, `FloatingButton.jsx`.
  - `src/components/Modals/` — `AddModal.jsx` (меню операций), формы: `ExpenseForm.jsx`, `IncomeForm.jsx`, `IncasationForm.jsx`, `TransferForm.jsx`, `EditModal.jsx`.
  - `src/components/UI/` — `SelectWithSearch.jsx` (поисковый селект).
  - `src/components/Analytics/` — `PivotTable.jsx`, `ChartsView.jsx`.

⸻

## ⚙️ 3. Логика и функционал

Основные модули и взаимодействия:
- Desktop (Tkinter):
  - Главный экран с кнопками (Timeline, быстрые операции, отчеты, справочники, аналитика, права).
  - Окно Timeline (read-only) показывает объединенную ленту операций из таблицы `timeline`.
  - Окно Quick Add добавляет операции напрямую в `timeline` (тип: expense/income).
  - Окна прав пользователей и логики входа (permissions) — скрывают/показывают элементы по правам.
  - Отчеты кассира — сбор сумм и платежей, запись методов оплаты в `daily_report_payments`.
- Telegram Bot:
  - FSM `DailyReport` и `ExpenseInput`: циклы “Добавить еще?” → запись в Audit/Timeline.
  - Права: динамическая клавиатура по permissions (например, `quick_add_expense`, `create_cashier_report`).
  - Быстрое добавление расходов/приходов → запись в `timeline` с `source='telegram'`.
- Backend (FastAPI):
  - Timeline:
    - GET `/timeline?limit=N` — последние операции.
    - PUT `/timeline/{id}` — обновление записи.
    - DELETE `/timeline/{id}` — удаление записи.
  - Операции:
    - POST `/operations/expense` — расход.
    - POST `/operations/income` — приход.
    - POST `/transfers/incasation` — инкасация (from_account → to_account).
    - POST `/transfers/transfer` — перевод (from_account → to_account + комиссия).
  - Справочники:
    - GET `/accounts` (активные), GET `/accounts/all`, POST/PUT/DELETE `/accounts`.
    - GET `/categories/expense` (активные), GET `/categories/expense/all`, POST/PUT/DELETE `/categories/expense`.
  - Аналитика:
    - GET `/analytics/dashboard?days=7` — KPI.
    - GET `/analytics/pivot` — сводная таблица расходов по категориям и месяцам.
    - GET `/analytics/trend?days=30` — тренды выручка/расходы/прибыль по дням.
- Frontend (Mini App):
  - Кнопка “+” (FloatingButton) открывает `AddModal` с выбором типа операции; формы используют `SelectWithSearch` и API.
  - Страница `Timeline` — загрузка через API, фильтры по типам операций, редактирование/удаление через `EditModal`.
  - Страница `Analytics` — табы: KPI Dashboard, PivotTable, Charts (Recharts).
  - Страница `Settings` — CRUD для категорий расходов и счетов.

Примеры обработок (псевдокод):

```text
Frontend createExpense():
  formData = { date, category_id, account_id, amount, description }
  POST /operations/expense → timeline.id
  обновить UI / перейти на Timeline
```

```text
Backend POST /operations/expense:
  INSERT INTO timeline (date, type='expense', category_id, amount, account_id, description, source='miniapp', user_id)
  SELECT * FROM timeline WHERE id = lastrowid → вернуть как JSON
```

```text
Analytics dashboard(days):
  revenue = SUM(income)
  food_cost = SUM(expense WHERE category LIKE 'продукт%|овощ%')
  labor_cost = SUM(expense WHERE category LIKE '%зарплат%')
  prime_cost = food_cost + labor_cost
  profitability = (revenue - total_expenses) / revenue * 100
  return {... KPI ...}
```

⸻

## 💾 4. Работа с базой данных

- **Тип БД**: SQLite (`finance_v5.db`).
- **Ключевые таблицы**:
  - `users(id, username, telegram_id, is_active, ...)`
  - `accounts(id, name, type, is_active, ...)`
  - `expense_categories(id, name, parent_id, is_active, ...)`
  - `income_categories(id, name, is_active, ...)`
  - `timeline(id, date, type, category_id, amount, account_id, description, source, user_id, from_account_id, to_account_id, commission_amount, ...)`
  - `daily_reports(...)` и `daily_report_payments(report_id, payment_method_id, amount)` — для отчетов кассира
  - `telegram_users(...)` — расширения для Telegram (например, `role`, `full_name`) при миграциях
- **Вью/представления**:
  - `timeline_view` — удобное представление для ленты (если применялась миграция).
- **Типовые запросы**:
  - Активные аккаунты: `SELECT * FROM accounts WHERE is_active = 1 ORDER BY name`
  - Активные категории расходов: `SELECT * FROM expense_categories WHERE is_active = 1 ORDER BY name`
  - Последние операции: `SELECT * FROM timeline ORDER BY date DESC, id DESC LIMIT ?`
  - Pivot (пример): группировка расходов по категориям и месяцам через `strftime('%Y-%m', date)`.

⸻

## 🔐 5. Авторизация и безопасность

- **Telegram Mini App / Backend**:
  - Клиент передает заголовок `X-Telegram-Id` (берется из Telegram WebApp API или временно — из `localStorage`).
  - Бэкенд находит/валидирует пользователя в таблице `users` и маппит к `user_id`.
  - На время разработки аутентификация может быть отключена: `Depends(lambda: 4)` подставляет `user_id=4`.
- **CORS**: включен `allow_origins=["*"]` для фронтенда.
- **Telegram Bot**:
  - Система прав (permissions) — проверка доступа к функционалу (формирование клавиатуры, запрет действий).
  - FSM с явными переходами и `await` для корректной обработки состояний и записи в `timeline`.
- **JWT/OAuth**: не используются (на текущем этапе).

⸻

## 🔧 6. Настройка и запуск

Backend:
- Зависимости (`backend/requirements.txt`): `fastapi`, `uvicorn[standard]`, `python-dotenv`
- Переменные окружения:
  - `FINANCE_DB_PATH` — путь к `finance_v5.db` (иначе берется абсолютный default в `backend/database.py`)
- Запуск:
  - Перейти в `backend/`
  - Установить зависимости: `pip install -r requirements.txt`
  - Старт: `uvicorn main:app --reload --port 8000`
  - Открыть Swagger: `http://localhost:8000/docs`

Frontend (Mini App):
- Перейти в `miniapp/frontend/`
- Установить зависимости: `npm install`
- Создать `.env`:
  - `VITE_API_URL=http://localhost:8000`
- Запуск Dev: `npm run dev`
- Telegram ID во фронтенде:
  - Интерсептор Axios добавляет `X-Telegram-Id` из `localStorage`.
  - Для теста: при старте `App.jsx` фиксирует `localStorage.setItem('telegram_id', '123456789')` (если нет user.id).

База данных:
- Файл `finance_v5.db` находится в корне проекта.
- Миграции (если применялись): `migration_timeline.sql`, `migration_hierarchy.sql` и скрипты `migrate_to_timeline.py`.
- Перед миграциями — сделать бэкап БД.

⸻

## 📊 7. Дополнительная информация

- **Бизнес-логика**:
  - Timeline — единая лента всех движений денег/операций.
  - Быстрое добавление доступно из Desktop/Telegram/Mini App, операции сразу попадают в `timeline`.
  - Отчеты кассира — агрегируют платежи (наличные/безнал/доставка и т.д.) с записью разбиений в `daily_report_payments`.
- **Аналитика**:
  - KPI (Dashboard): выручка, Food Cost, Labor Cost, Prime Cost, прибыль, рентабельность; подсветка по порогам.
  - Pivot Table: расходы по категориям и месяцам (сводная таблица).
  - Trend Charts (Recharts): выручка/расходы/прибыль по дням.
- **UI/UX (Mini App)**:
  - Поисковый селект `SelectWithSearch` для длинных списков.
  - Центрированная плавающая кнопка (FAB) и хаптика в Telegram среде.
  - Редактирование и удаление прямо из `Timeline` через `EditModal`.
- **Права и авторизация**:
  - Desktop: окно входа + фильтрация видимости кнопок по правам.
  - Telegram: клавиатура по правам, FSM-потоки для быстрых операций.
- **Известные решения/фиксы** (важные):
  - Устранены проблемы с путями БД и относительными импортами.
  - Поля `date` в Pydantic — строки с `isoformat()` для корректной сериализации.
  - Временное отключение Auth (заглушка `Depends(lambda: 4)`) для тестирования фронтенда.
  - Исправлена запись методов оплаты в `daily_report_payments`.
  - Мини-приложение: устранены циклы загрузки, пустые экспортные компоненты, ошибки с import default.

⸻

## Вывод

Этот документ описывает архитектуру и ключевые модули AIR_WAFFLE_FINANCE так, чтобы другой ИИ-ассистент мог быстро восстановить функционал и структуру системы без доступа к исходникам. Сфокусируйтесь на:
- едином источнике данных `finance_v5.db` (SQLite),
- API FastAPI как контракте для Mini App,
- общей шине событий `timeline`,
- правах доступа (permissions) в Desktop/Telegram,
- аналитике (KPI, pivot, тренды) для управленческих решений.



