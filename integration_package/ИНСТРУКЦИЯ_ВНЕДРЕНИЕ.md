# 🚀 ИНСТРУКЦИЯ ПО ВНЕДРЕНИЮ НОВОЙ СИСТЕМЫ

## 📋 ЧТО СДЕЛАНО:

### ✅ 1. ИЕРАРХИЯ КАТЕГОРИЙ
- Неограниченная вложенность
- Дерево категорий
- Удобный интерфейс

### ✅ 2. ГРУППЫ ДЛЯ АНАЛИТИКИ
- Food Cost, Labor Cost, Overhead, Marketing, Other
- Настраиваемый маппинг категорий
- Автоматический расчёт %

### ✅ 3. АНАЛИТИКА
- Дашборд с графиками
- Структура расходов
- Прибыльность
- Таблица по дням

---

## 📦 ФАЙЛЫ:

1. **migration_hierarchy.sql** - SQL миграция БД
2. **category_manager.py** - Управление категориями
3. **analytics.py** - Модуль аналитики
4. **add_categories.sql** - Добавление категорий из Google Sheets
5. **add_categories_from_sheets.py** - Python скрипт импорта

---

## 🔧 УСТАНОВКА:

### ШАГ 1: ОБНОВИТЬ БД

```bash
# На Mac в папке проекта
sqlite3 finance_v5.db < migration_hierarchy.sql
```

**ИЛИ загрузи на Railway:**

```bash
# Через Railway CLI или панель управления
# Выполни SQL из migration_hierarchy.sql
```

---

### ШАГ 2: ДОБАВИТЬ МОДУЛИ В DESKTOP

**В `main_app.py` добавь кнопку (строка ~78):**

```python
("📂 Категории (новые)", self.manage_categories_new, 2, 0),
("📊 Аналитика", self.show_analytics, 2, 3),
```

**Добавь методы (~строка 200):**

```python
def manage_categories_new(self):
    """Управление категориями с иерархией"""
    from category_manager import CategoryManager
    CategoryManager(self.db)

def show_analytics(self):
    """Показать аналитику"""
    from analytics import AnalyticsWindow
    AnalyticsWindow(self.db)
```

---

### ШАГ 3: ДОБАВИТЬ КАТЕГОРИИ ИЗ GOOGLE SHEETS

**Вариант A - через Desktop:**
- Открой "📂 Категории (новые)"
- Создай вручную все категории

**Вариант B - через Python:**
```bash
python3 add_categories_from_sheets.py
```

---

### ШАГ 4: НАСТРОИТЬ ГРУППЫ

1. Открой "📂 Категории (новые)"
2. Вкладка "Группы для аналитики"
3. Для каждой группы жми "⚙️ Настроить"
4. Перетаскивай категории в группы:
   - **Food Cost**: Базар, Фирма закуп, Корзинка, Хавас, Бар...
   - **Labor Cost**: Зарплата, Авансы, Обед
   - **Overhead**: Аренда, Налоги, Хоз товары
   - **Marketing**: Ивенты, Маркетинг
   - **Other**: всё остальное

---

### ШАГ 5: ИНВЕНТАРИЗАЦИЯ (первый запуск)

**2 ноября вечером:**

1. Физически посчитай:
   - Наличные в кассе: ___________
   - Остаток на р/с: ___________
   
2. Внеси начальные остатки:
   - Desktop → "💰 Балансы счетов"
   - Выбери счёт → "Исправить баланс"
   - Укажи фактический остаток

3. Создай первый отчёт через Mini App! 🎉

---

## 🎯 РЕЗУЛЬТАТ:

После внедрения получаешь:

✅ Иерархическая структура категорий  
✅ Аналитика с процентами (Food Cost, Labor...)  
✅ Визуальные графики  
✅ Детализация по дням  
✅ Единая БД (Desktop + Mini App + Telegram)  
✅ Автоматические расчёты  

---

## 🆘 ПОДДЕРЖКА:

Если что-то не работает - пиши!

