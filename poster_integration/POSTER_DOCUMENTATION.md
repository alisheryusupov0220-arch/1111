# 📡 POSTER API ИНТЕГРАЦИЯ - ПОЛНАЯ ДОКУМЕНТАЦИЯ

## 🎯 ЧТО ЭТО:

Автоматическая синхронизация ингредиентов из Poster в систему учёта.

---

## 🔄 КАК РАБОТАЕТ:

```
[POSTER] (ресторанная система)
    ↓
    ↓ API (каждые 6 часов)
    ↓
[sync_poster.py] ← автоматическая синхронизация
    ↓
[БД finance_v5.db]
    ↓
    ├─ Desktop (все категории)
    └─ Mini App (только видимые для кассира)
```

---

## 📊 СТРУКТУРА БД:

```sql
expense_categories:
├─ poster_id                 (ID из Poster)
├─ poster_type               ('ingredient' или 'product')
├─ is_active_in_poster       (1 = есть в Poster, 0 = удалён)
├─ visible_for_cashier       (1 = видит кассир, 0 = скрыт)
└─ synced_at                 (дата последней синхронизации)

poster_settings:
├─ api_token                 (токен API)
├─ api_url                   (URL Poster API)
├─ sync_interval_hours       (интервал синхронизации)
└─ last_sync_at              (последняя синхронизация)

poster_sync_logs:
├─ sync_date                 (дата)
├─ items_added               (добавлено)
├─ items_updated             (обновлено)
├─ items_deactivated         (деактивировано)
└─ status                    (success/error)
```

---

## 🚀 УСТАНОВКА:

### ШАГ 1: МИГРАЦИЯ БД

```bash
sqlite3 finance_v5.db < migration_poster.sql
```

Это создаст:
- Колонки для Poster в категориях
- Таблицу настроек
- Таблицу логов

---

### ШАГ 2: УСТАНОВКА ЗАВИСИМОСТЕЙ

```bash
pip install requests apscheduler --break-system-packages
```

---

### ШАГ 3: КОПИРОВАНИЕ ФАЙЛОВ

Скопируй в корень проекта:
- `sync_poster.py` - модуль синхронизации
- `poster_scheduler.py` - планировщик
- `poster_settings_ui.py` - UI настроек
- `api_categories_poster.py` - API эндпоинты

---

### ШАГ 4: ИНТЕГРАЦИЯ В DESKTOP

В `main_app.py`:

1. Добавь кнопку "📡 Poster API"
2. Добавь метод `open_poster_settings()`
3. Запусти scheduler при старте

---

### ШАГ 5: ИНТЕГРАЦИЯ В API

В `api_server.py`:

1. Подключи `categories_router`
2. Добавь события `startup` и `shutdown`
3. Запусти scheduler

---

## ⚙️ НАСТРОЙКА:

### ПЕРВАЯ НАСТРОЙКА:

1. Открой Desktop → "📡 Poster API"
2. Заполни поля:

```
API Token: 446581:5788638578facbc50916f9f175303637
API URL: https://joinposter.com/api/
Supplier ID: 1
Storage ID: 1
Синхронизация: 6 часов
```

3. Поставь галочку "✅ Включить автосинхронизацию"
4. Жми "💾 Сохранить"

---

### ТЕСТ ПОДКЛЮЧЕНИЯ:

1. Жми "🧪 Тест подключения"
2. Должно показать: "✅ Подключение успешно! Найдено ингредиентов: XXX"

---

### ПЕРВАЯ СИНХРОНИЗАЦИЯ:

1. Жми "🔄 Синхронизировать сейчас"
2. Подожди 10-30 секунд
3. Должно показать:
```
✅ Синхронизация завершена!

Добавлено: XXX
Обновлено: 0
Деактивировано: 0
```

---

## 🎛️ УПРАВЛЕНИЕ ВИДИМОСТЬЮ:

### ДЛЯ КАССИРА:

Кассир видит только категории где:
- `is_active = 1` (активная)
- `visible_for_cashier = 1` (видимая)
- `is_active_in_poster = 1` (есть в Poster)

### СКРЫТЬ КАТЕГОРИЮ:

Вариант 1 - через Desktop:
```
Категории → Правый клик → "Скрыть от кассира"
```

Вариант 2 - через API:
```bash
POST /api/categories/{id}/toggle_visibility
{
  "visible": false
}
```

---

## 📱 API ЭНДПОИНТЫ:

### Получить категории для кассира:
```
GET /api/categories/for_cashier

Response:
{
  "success": true,
  "data": [...],
  "count": 123
}
```

### Все категории (для менеджера):
```
GET /api/categories/all?include_inactive=false

Response:
{
  "success": true,
  "data": [...],
  "count": 150
}
```

### Переключить видимость:
```
POST /api/categories/42/toggle_visibility
{
  "visible": false
}

Response:
{
  "success": true,
  "message": "Visibility updated"
}
```

### Статус Poster:
```
GET /api/categories/poster_status

Response:
{
  "poster_enabled": true,
  "last_sync": "2025-11-03 10:00:00",
  "sync_interval_hours": 6,
  "stats": {
    "total": 150,
    "active_in_poster": 145,
    "visible_for_cashier": 120,
    "from_poster": 145
  },
  "recent_logs": [...]
}
```

### Запустить синхронизацию:
```
POST /api/categories/sync_now

Response:
{
  "success": true,
  "added": 5,
  "updated": 10,
  "deactivated": 2
}
```

---

## 🔄 ЛОГИКА СИНХРОНИЗАЦИИ:

### ЧТО ПРОИСХОДИТ:

1. **Получение данных из Poster:**
   - `menu.getIngredients` → все ингредиенты
   - `menu.getProducts` → продукты type="3"

2. **Сравнение с БД:**
   - Есть в БД → **обновить** (название, дата)
   - Нет в БД → **добавить** (новая категория)
   - Нет в Poster → **деактивировать** (не удалять!)

3. **Результат:**
   - Новые ингредиенты автоматически видны кассиру
   - Удалённые ингредиенты скрыты от кассира
   - История сохранена (для старых отчётов)

---

## 📝 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:

### СЦЕНАРИЙ 1: Новый ингредиент

```
День 1:
- В Poster добавили "Моцарелла новая"

День 1 (через 6 часов):
- Автосинхронизация
- "Моцарелла новая" в БД
- visible_for_cashier = 1
- Кассир сразу видит в Mini App!
```

---

### СЦЕНАРИЙ 2: Удалили из Poster

```
День 1:
- В Poster удалили "Старый сыр"

День 1 (через 6 часов):
- Автосинхронизация
- "Старый сыр": is_active_in_poster = 0
- Кассир НЕ видит
- В старых отчётах ЕСТЬ!
```

---

### СЦЕНАРИЙ 3: Скрыли от кассира

```
Менеджер:
- Открыл Desktop → Категории
- Правый клик на "Дорогой трюфель"
- "Скрыть от кассира"

Результат:
- visible_for_cashier = 0
- Кассир не видит
- Менеджер может добавить сам через Desktop
```

---

## 🔧 TROUBLESHOOTING:

### Синхронизация не работает:

**Проблема:** Ничего не синхронизируется

**Решение:**
1. Проверь настройки: Desktop → "📡 Poster API"
2. Убедись что галочка "✅ Включить" стоит
3. Проверь API token (не `YOUR_TOKEN_HERE`)
4. Жми "🧪 Тест подключения"

---

### Кассир не видит категории:

**Проблема:** В Poster есть, но кассир не видит

**Решение:**
1. Проверь флаги категории:
   - `is_active = 1` ✅
   - `visible_for_cashier = 1` ✅
   - `is_active_in_poster = 1` ✅
2. Запусти синхронизацию вручную
3. Проверь API: `/api/categories/for_cashier`

---

### Ошибка подключения к Poster:

**Проблема:** "❌ Не удалось подключиться"

**Решение:**
1. Проверь интернет
2. Проверь API token
3. Проверь URL: `https://joinposter.com/api/`
4. Проверь логи: `poster_sync_logs`

---

## 📊 МОНИТОРИНГ:

### Через Desktop:

```
Desktop → "📡 Poster API"
→ Внизу таблица "История синхронизации"
→ Последние 20 синхронизаций
```

### Через БД:

```sql
-- Статистика
SELECT 
    COUNT(*) as total,
    SUM(CASE WHEN is_active_in_poster = 1 THEN 1 ELSE 0 END) as active,
    SUM(CASE WHEN visible_for_cashier = 1 THEN 1 ELSE 0 END) as visible
FROM expense_categories;

-- Последние логи
SELECT * FROM poster_sync_logs 
ORDER BY sync_date DESC 
LIMIT 10;
```

---

## 🚀 ДЕПЛОЙ НА RAILWAY:

### После локального теста:

```bash
git add .
git commit -m "Add Poster API integration"
git push
```

Railway автоматически:
1. Обновит БД (миграция)
2. Установит зависимости
3. Запустит scheduler
4. Начнёт автосинхронизацию!

---

## ✅ CHECKLIST ВНЕДРЕНИЯ:

- [ ] Миграция БД выполнена
- [ ] Файлы скопированы
- [ ] main_app.py обновлён
- [ ] api_server.py обновлён
- [ ] requirements.txt обновлён
- [ ] Локально работает
- [ ] API token настроен
- [ ] Тест подключения ✅
- [ ] Первая синхронизация ✅
- [ ] Кассир видит категории
- [ ] Задеплоено на Railway
- [ ] Scheduler работает

---

## 🎯 РЕЗУЛЬТАТ:

После внедрения:

✅ Автоматическая синхронизация с Poster  
✅ Новые ингредиенты добавляются автоматически  
✅ Удалённые скрываются от кассира  
✅ История сохранена  
✅ Контроль видимости для кассира  
✅ API для Mini App  
✅ Логи и мониторинг  

---

**ГОТОВО!** 🚀
