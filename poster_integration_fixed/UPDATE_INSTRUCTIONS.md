# 🔄 КАК ОБНОВИТЬ POSTER API (ИСПРАВЛЕНИЯ)

## 🐛 ЧТО БЫЛО НЕПРАВИЛЬНО:

- ❌ Токен не сохранялся (нужно было вводить каждый раз)
- ❌ Токен скрыт звёздочками (неудобно проверять)
- ❌ Нет статуса сохранения

---

## ✅ ЧТО ИСПРАВЛЕНО:

- ✅ Токен сохраняется в БД правильно
- ✅ Видно статус: "Сохранён: 4465...7637"
- ✅ Кнопка 🔒/👁️ показать/скрыть токен
- ✅ Не нужно вводить каждый раз!
- ✅ Лучшие сообщения об ошибках

---

## 🚀 ОБНОВЛЕНИЕ ЗА 2 МИНУТЫ:

### ВАРИАНТ 1: АВТОМАТИЧЕСКИ (CURSOR)

1. Распакуй `poster_integration_fixed.tar.gz`
2. Скопируй в Cursor Chat:

```
Обнови эти файлы в проекте:
- poster_settings_ui.py
- sync_poster.py  
- migration_poster.sql

Замени их на новые версии из архива poster_integration_fixed.tar.gz
```

3. Cursor заменит файлы автоматически!

---

### ВАРИАНТ 2: ВРУЧНУЮ

```bash
# 1. Распакуй архив
tar -xzf poster_integration_fixed.tar.gz

# 2. Скопируй исправленные файлы в проект:
cp poster_settings_ui.py /path/to/project/
cp sync_poster.py /path/to/project/
cp migration_poster.sql /path/to/project/

# 3. Если БД уже мигрирована, обнови запись:
sqlite3 finance_v5.db << 'SQL'
UPDATE poster_settings 
SET api_token = '',
    api_url = 'https://joinposter.com/api/',
    supplier_id = '1',
    storage_id = '1',
    sync_interval_hours = 6
WHERE id = 1;
SQL

# 4. Готово!
```

---

## 🎮 ПРОВЕРКА:

После обновления:

```
1. Открой Desktop → "📡 Poster API"
2. Введи токен: 446581:5788638578facbc50916f9f175303637
3. Жми "💾 Сохранить"
4. Должен появиться: "Сохранён: 4465...7637" ✅
5. Закрой окно
6. Открой снова → Токен УЖЕ ЗАГРУЖЕН! 🎉
```

---

## 📋 ИЗМЕНЁННЫЕ ФАЙЛЫ:

✅ **poster_settings_ui.py**
- Токен виден по умолчанию
- Кнопка показать/скрыть работает
- Статус токена
- Улучшенная валидация
- Исправлено сохранение (INSERT или UPDATE)

✅ **sync_poster.py**
- Лучшая обработка ошибок
- Информативные логи
- Проверка токена

✅ **migration_poster.sql**
- Убрано 'YOUR_TOKEN_HERE'
- Правильная начальная запись

✅ **CURSOR_POSTER_PROMPT.txt**
- Обновлённая инструкция

---

## 🎯 РЕЗУЛЬТАТ:

До исправления:
```
1. Открываешь настройки → токен пустой 😢
2. Вводишь длинный токен
3. Сохраняешь
4. Закрываешь
5. Открываешь снова → ТОКЕН ОПЯТЬ ПУСТОЙ! 😡
6. Нужно вводить заново...
```

После исправления:
```
1. Открываешь настройки → токен пустой
2. Вводишь токен ОДИН РАЗ
3. Сохраняешь → "Сохранён: 4465...7637" ✅
4. Закрываешь
5. Открываешь снова → ТОКЕН УЖЕ ТАМ! 🎉
6. Больше не нужно вводить!
```

---

## 🆘 ЕСЛИ ПРОБЛЕМЫ:

### "Токен всё равно не сохраняется"

→ Проверь права на файл БД:
```bash
ls -l finance_v5.db
chmod 664 finance_v5.db
```

### "Ошибка при сохранении"

→ Проверь что миграция выполнена:
```bash
sqlite3 finance_v5.db "SELECT * FROM poster_settings;"
```

### "Старый код ещё работает"

→ Перезапусти приложение:
```bash
# Закрой полностью Desktop
python3 main_app.py
```

---

**ГОТОВО!** 🎉

Токен теперь сохраняется правильно!
